//  https://leetcode.com/problems/plus-one/

class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        res = ""
        for i in digits:
            res += str(i)
        res = int(res)+1
        l = []
        for i in str(res):
            l.append(int(i))
        return l
        
