//  https://leetcode.com/problems/add-binary/

class Solution:
    def addBinary(self, a: str, b: str) -> str:
        s1 = int(a, 2)
        s2 = int(b, 2)
        r = int(s1)+int(s2);
        return bin(r).replace("0b","")
        
